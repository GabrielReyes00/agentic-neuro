---
name: inbox-workflow
description: Use when Gabriel invokes /inbox-workflow or asks to triage email and draft replies with approval before any external mutation.
---

# Inbox Workflow

Resolve the plugin root from this skill directory. Read
`../../resources/AGENTS.md`, `../../resources/.agents/shared/runtime/inbox-workflow.json`,
and `../../resources/.agents/shared/commands/workflow-runtime.md`, then the entry
contracts: `../../resources/.agents/shared/commands/inbox-workflow.md`. These generated mirrors preserve the canonical
`.agents/shared/commands/` behavior. Load later contracts only after a declared
transition.
