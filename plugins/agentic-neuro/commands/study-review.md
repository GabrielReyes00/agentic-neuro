---
description: Memory-enabled Socratic review from a vault document or weak-spot queue.
argument-hint: [topic-or-document-or-review-request]
---

# Study Review

The user invoked `/study-review` with: $ARGUMENTS

Read and follow `.agents/shared/commands/study-review.md`.

Use `.agents/shared/commands/learning-session-contract.md` for memory operations, Adaptive Teaching Doctrine, Anki Card Doctrine, and session-end integrity. Use `.agents/shared/commands/anki-card-quality.md` for card drafting, validation, deck taxonomy, and duplicate judgment.

This command writes no vault artifact. The memory layer is the durable record.
