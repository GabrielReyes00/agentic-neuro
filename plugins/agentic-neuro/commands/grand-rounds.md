---
description: Build and validate an editable neurosurgery case or article presentation package.
argument-hint: --mode <case|article> [source options]
---

# Grand Rounds

The user invoked `/grand-rounds` with: $ARGUMENTS

Resolve the plugin root from this command file. Read `resources/AGENTS.md`,
`resources/.agents/shared/runtime/grand-rounds.json`, and
`resources/.agents/shared/commands/workflow-runtime.md`, then the entry
contracts: `resources/.agents/shared/commands/grand-rounds.md`, `resources/.agents/shared/commands/vault-intelligence.md`. These are generated mirrors of the canonical
`.agents/shared/commands/` contracts. Load later contracts only after a declared
transition.
