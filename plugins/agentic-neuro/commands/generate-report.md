---
description: Generate an encyclopedic citation-dense neurosurgical reference report in the Obsidian vault.
argument-hint: [topic]
---

# Generate Report

The user invoked `/generate-report` with: $ARGUMENTS

Read and follow `.agents/shared/commands/generate-report.md`.

Write the report to `Reports/<Title>.md`, include `## Mastery Objectives`, validate the target report with `src/report_validator.py`, and log the report anchor to memory per the shared command.
