---
description: Audit learner-memory identity, telemetry, graph, and database health; apply reviewed repairs only with approval.
argument-hint: [audit|merge-topics|load-reference-graph|maintain|knowledge-map]
---

# Memory Maintenance

The user invoked `/memory-maintenance` with: $ARGUMENTS

Resolve the plugin root from this command file. Read `resources/AGENTS.md`,
`resources/.agents/shared/runtime/memory-maintenance.json`, and
`resources/.agents/shared/commands/workflow-runtime.md`, then the entry
contracts: `resources/.agents/shared/commands/memory-maintenance.md`, `resources/.agents/shared/commands/memory-curation.md`. These are generated mirrors of the canonical
`.agents/shared/commands/` contracts. Load later contracts only after a declared
transition.
