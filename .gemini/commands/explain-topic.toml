description = "Expert mechanistic explanation at brief, standard, or deep depth — precise mental model building"

prompt = """
ACTIVE COMMAND: /explain-topic — Domain-agnostic expert explanation.

Deliver a structured explanation using the format in the instruction file. Parse depth flags (--brief, --standard, --deep) and --save from the user input. If topic is ambiguous across domains, ask one clarifying question first.

Critical pre-step: run the learner-context prepass (`knowledge_graph.py context`) before drafting the explanation.

User input: {{args}}

---

@{.gemini/commands/explain-topic.md}
"""
