description = "Adaptive 30-minute study session — recall, remediation, new territory, transfer"

prompt = """
ACTIVE COMMAND: /study-session — Adaptive session from memory gaps.

MANDATORY hooks: recall before plan, log-answer after each answer, Obsidian write, end-session at completion.
Invisible bookkeeping: do not print shell commands or raw outputs for these hooks.

FINAL ARTIFACT GATE: log-answer calls alone are not completion. Write a rich draft to data/Sessions/study_session_<slug>_artifact.md and install/validate it with src/learning_artifact_guard.py --artifact-type study-session before claiming the Review Sessions note exists.

User's request: {{args}}

---

@{.gemini/commands/study-session.md}
"""
