description = "Answer a bounded bedside task or immediate clinical decision; durable capture is optional."

prompt = """
ACTIVE COMMAND: /consult
User input: {{args}}

Read the generated selected-workflow spec below, then
`.agents/shared/commands/workflow-runtime.md`, then the entry contracts:
`.agents/shared/commands/consult.md`. Load later contracts only after a declared
transition. Do not infer behavioral policy from this adapter.

@{.agents/shared/runtime/consult.json}
"""
