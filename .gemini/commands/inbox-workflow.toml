description = "Agentic email triage — fetch inbox, categorize, extract action items, draft responses with approval gate"

prompt = """
ACTIVE COMMAND: /inbox-workflow — Multi-phase Exchange inbox triage pipeline.

HARD CONSTRAINT: No email may be sent or drafted for sending without explicit user review and approval. This constraint cannot be overridden.

This is a multi-phase workflow (fetch -> categorize -> action items -> response drafting). Follow ALL phases in the instruction file. Email categorization is delegated to a sub-task. Raw email bodies stay in the sub-task context — only structured JSON enters the main context.

User's request: {{args}}

---

@{.gemini/commands/inbox-workflow.md}
"""
