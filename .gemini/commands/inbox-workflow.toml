description = "Agentic email triage — fetch, categorize, action items, draft with approval gate"

prompt = """
ACTIVE COMMAND: /inbox-workflow — Exchange inbox triage.

HARD CONSTRAINT: No email sent without explicit user approval. Cannot be overridden. Raw bodies stay in sub-task; structured JSON only in main context.

User's request: {{args}}

---

@{.gemini/commands/inbox-workflow.md}
"""
