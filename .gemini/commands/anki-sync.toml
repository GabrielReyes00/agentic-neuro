description = "Extract flashcards from session and sync to Anki via AnkiConnect"

prompt = """
ACTIVE COMMAND: /anki-sync — 9-step sequential card pipeline.

MANDATORY: Step 6 blind validation must complete on every card before writing final_cards.json. Step 6.5 CLI gate must pass before dispatch. Step 9 hooks (KG, Obsidian, cleanup) never skipped.

User's request: {{args}}

---

@{.gemini/commands/anki-sync.md}
"""
