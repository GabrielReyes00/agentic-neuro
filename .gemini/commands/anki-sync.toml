description = "Manual Anki card flush, review, or creation via anki_queue.py"

prompt = """
ACTIVE COMMAND: /anki-sync

Manual flush, review, or card creation for the Anki pipeline. All card work uses `anki_queue.py` — review, check for duplicates, then flush.

User's request: {{args}}

---

@{.agents/shared/commands/anki-sync.md}
"""
