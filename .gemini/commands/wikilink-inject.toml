description = "Inject Obsidian wikilinks into Case Log notes — matches concepts to existing vault entries"

prompt = """
ACTIVE COMMAND: /wikilink-inject — Obsidian wikilink injection for Case Log notes.

Locate the specified Case Log file, scan it for concepts matching existing vault notes, then rewrite the note with inline wikilinks. Do NOT modify clinical content — only add wikilink syntax around matching terms.

User's request: {{args}}

---

@{.gemini/commands/wikilink-inject.md}
"""
