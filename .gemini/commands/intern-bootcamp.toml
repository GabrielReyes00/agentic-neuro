description = "Neurosurgery intern simulation — night float scenarios, order drills, chief debrief"

prompt = """
ACTIVE COMMAND: /intern-bootcamp — PGY-1 simulation with mandatory hooks.

Silent/background steps are NOT optional — execute without narrating. MANDATORY: preflight, heartbeat every ~3 decisions, log_bootcamp after debrief, Obsidian write, concept extraction, post-session hook (final step).

User's request: {{args}}

---

@{.gemini/commands/intern-bootcamp.md}
"""
